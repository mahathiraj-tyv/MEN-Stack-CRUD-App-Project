<nav class="navbar navbar-expand-lg navbar-dark shadow-sm"
     style="background-color: #334155;">
  <div class="container">

    <!-- Brand -->
    <a class="navbar-brand fw-bold text-info" href="/">
      🚌 TravelZap
    </a>

    <!-- Right Side -->
    <div class="ms-auto">

      <% if (!userId) { %>

        <a href="/login" class="btn btn-info me-2">
          Login
        </a>

        <a href="/register" class="btn btn-outline-info">
          Register
        </a>

      <% } else { %>

        <% if (userRole === 'admin') { %>
          <a href="/admin/dashboard" class="btn btn-outline-info me-2">
            Admin Dashboard
          </a>
        <% } else { %>
          <a href="/customer/dashboard" class="btn btn-outline-info me-2">
            Dashboard
          </a>
        <% } %>

        <a href="/logout" class="btn btn-info">
          Logout
        </a>

      <% } %>

    </div>
  </div>
</nav>